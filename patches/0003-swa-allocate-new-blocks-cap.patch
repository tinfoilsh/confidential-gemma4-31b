--- a/usr/local/lib/python3.12/dist-packages/vllm/v1/core/single_type_kv_cache_manager.py
+++ b/usr/local/lib/python3.12/dist-packages/vllm/v1/core/single_type_kv_cache_manager.py
@@ -640,6 +640,25 @@
         """
         return max(0, num_computed_tokens - self.sliding_window + 1)
 
+    # Tinfoil patch (0003): cap num_tokens in the actual allocation path too.
+    # The base class's allocate_new_blocks computes
+    #     num_required_blocks = cdiv(num_tokens, block_size)
+    # using the uncapped sequence length. After remove_skipped_blocks() has
+    # trimmed req_to_blocks down to the window, this mismatch can cause
+    # allocate_new_blocks to request 1+ blocks from the pool even when the
+    # coordinator's (capped) get_num_blocks_to_allocate said "0 more needed",
+    # tripping BlockPool.get_new_blocks() with a ValueError. Capping here
+    # mirrors get_num_blocks_to_allocate so admission and allocation stay
+    # consistent. Companion to the admission-math cap in patch 0002.
+    def allocate_new_blocks(
+        self, request_id: str, num_tokens: int, num_tokens_main_model: int
+    ) -> list[KVCacheBlock]:
+        return super().allocate_new_blocks(
+            request_id,
+            self._cap_num_tokens(num_tokens),
+            self._cap_num_tokens(num_tokens_main_model),
+        )
+
     def get_num_common_prefix_blocks(self, running_request_id: str) -> int:
         """
         NOTE(Chen): The prefix blocks are null blocks for sliding window layers.
